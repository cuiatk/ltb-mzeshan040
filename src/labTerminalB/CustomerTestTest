package labTerminalB;

import static org.junit.Assert.*;

import org.junit.Test;

public class CustomerTestTest {

	@Test
	public void test() {
		final Movie Spancer=new Movie( "the Spencer",Movie.CHILDREN);
		final Movie Speed=new Movie("the Speed",Movie.NEW_RELEASE);
	    final Movie Diamond=new Movie(" blood MAN",Movie.NEW_RELEASE);
		
		final Customer customer=new Customer("zee");
		
		@Test
		public void basicChildrenRental(){
			customer.addRental(new Rental(Spancer,2));	
			assertThat(customer.statement(),is(expectedMessageFor("the Spancer",1.2,1.5,1)));
			
			public void shouldDiscountRegularRental(){
				customer.addRental(new Rentsl(the speed,4));
				assertThat(customer.statement(),is(expectedMessageFor("the speed",5.0,5.0,1)));
				
			}
			
		}
	}

	private void basicChildrenRental() {
		// TODO Auto-generated method stub
		
	}

}
